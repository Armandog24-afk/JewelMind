fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
cadquery>=2.5
structlog>=24.1
python-multipart>=0.0.9

# Optional Designer v1 AI provider (backend/jewelmind/designer/provider.py).
# Only imported when DESIGNER_PROVIDER=anthropic and ANTHROPIC_API_KEY are
# both set; the rest of the app, including Designer's deterministic
# pipeline and FakeDesignerProvider-backed tests, works without it.
anthropic>=0.40

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.4
jsonschema>=4.20
