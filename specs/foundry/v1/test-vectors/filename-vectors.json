{
  "description": "Real sanitize_filename() outputs, generated by running the actual function during Sprint 7 — checked into backend/tests/test_filenames.py.",
  "vectors": [
    { "input": "My Ring-01.v2", "output": "My_Ring-01.v2" },
    { "input": "", "default": "jewelmind-export", "output": "jewelmind-export" },
    { "input": "   ", "default": "jewelmind-export", "output": "jewelmind-export" },
    { "input": "...", "default": "jewelmind-export", "output": "jewelmind-export" },
    { "input": ".hidden", "outputStartsWithDot": false },
    { "input": "-rm -rf", "outputStartsWithDash": false },
    { "input": "CON", "output": "CON", "note": "Windows reserved device name passes through unmodified — a known, documented gap affecting only the client-side suggested download filename, never a server-side path." },
    { "inputLength": 500, "inputChar": "a", "outputLength": 120, "note": "Max length cap enforced." },
    { "input": "a:b*c?d\"e<f>g|h", "note": "Every Windows-reserved path character (: * ? \" < > |) is replaced." }
  ]
}
