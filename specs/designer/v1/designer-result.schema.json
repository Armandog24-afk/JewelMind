{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://jewelmind.example/specs/designer/v1/designer-result.schema.json",
  "title": "Designer Result v1",
  "description": "The full response body of POST /api/designer/interpret. See backend/jewelmind/designer/schemas.py::DesignerResult.",
  "type": "object",
  "additionalProperties": false,
  "required": ["requestId", "proposal"],
  "properties": {
    "requestId": { "type": "string" },
    "proposal": {
      "$comment": "Same shape as design-proposal.schema.json, inlined at validation time by backend/tests/test_designer_schemas.py rather than via $ref, matching every prior sprint's specs/*/v1/ convention.",
      "type": "object"
    }
  }
}
