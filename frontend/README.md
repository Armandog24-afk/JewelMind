# JewelMind frontend

React + TypeScript (strict) + Vite + React Three Fiber. See the
repository-root [README.md](../README.md) and [docs/](../docs) for the full
product documentation, architecture, and setup instructions.

Quick start: `npm install && npm run dev` (http://localhost:3000). Tests:
`npm run test`. Build: `npm run build`.
