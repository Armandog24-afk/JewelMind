---
id: JM-BIBLE-SPRINT5-REPORT
title: Sprint 5 Validation Report
version: 1.0.0
status: accepted
owner: JewelMind
last_updated: 2026-08-24
source_of_truth: true
depends_on:
  - JM-BIBLE-ATLAS-README
related_documents: []
implementation_status: current
professional_validation: not_required
normative: false
---

# Sprint 5 Validation Report

## Atlas documents created

`docs/bible/07-atlas/README.md` plus 32 numbered documents (`120`–`151`), plus this report. 7 new appendices: `atlas-component-catalog.md`, `atlas-operation-catalog.md`, `atlas-coordinate-reference.md`, `atlas-geometry-invariant-catalog.md`, `atlas-inspection-catalog.md`, `atlas-fallback-register.md`, `atlas-code-mapping.md`.

## Machine-readable specifications created

`specs/atlas/v1/README.md`, 6 JSON Schemas (`geometry-component`, `geometry-assembly`, `geometry-metadata`, `geometry-inspection-result`, `geometry-error`, `component-manifest`), 5 real example records (generated by running `build_solitaire_ring(default_definition())` and `write_component_previews()` directly — no fabricated measurements), 5 test-vector files (`coordinate-vectors.json`, `transform-vectors.json`, `metadata-vectors.json`, `inspection-vectors.json`, `component-manifest-vectors.json`). New test file: `backend/tests/test_atlas_registry.py` (5 test cases).

## Geometry components discovered

**4**: `band`, `stone_reference`, `prongs`, `basket_support` — plus one assembly-level derived shape (`combined_metal`) that is not itself a named component. All four confirmed by direct inspection of `backend/jewelmind/geometry/components/*.py` and cross-checked against `geometry/assemblies/solitaire.py`.

## Geometry operations discovered

**14 used**: workplane creation, sketch/profile creation, revolve, extrude, loft, cylinder (circle+extrude composition), translation, rotation, union/fuse, cut, fillet, tessellation, STEP export, STL export. **2 never used anywhere**: sweep, intersection.

## Coordinate system

**Confirmed: yes.** World origin, finger-hole axis (global Y), band plane (X/Z), ring top, assembly anchor axis, stone center, prong angular origin, and rotation direction were all confirmed directly against `band.py`, `stone.py`, `prongs.py`, `basket.py`, and `geometry/constants.py`. **No inconsistency was found** across any of the four component builders — all share the same Z-reference (`band_top_z`, `EMBED_MM`) and, for prongs/basket, the same radial reference (`prong_center_radius`).

## Geometry fallbacks discovered

**2**: the band outer-rim fillet fallback (falls back to sharp edges on failure) and the combined-metal boolean fuse fallback (falls back to a 3-solid compound on failure). Both were already partially documented in `docs/known-limitations.md`/`docs/geometry-conventions.md`; this Sprint formalizes them into `docs/bible/appendices/atlas-fallback-register.md` with full trigger/behavior/warning/test/risk fields. **Neither fallback currently has a regression test that forces it to trigger** — confirmed by grepping `backend/tests/` for any test that exercises the failure path.

## Undocumented magic numbers found

**7**, none previously stated anywhere in the Bible or `docs/`: `_CULET_RADIUS_MM` (0.05mm, `stone.py`), `_COMFORT_FLARE_MM` (0.3mm, `band.py`), `_FILLET_FRACTION` (0.15, `band.py`), `_FILLET_MAX_MM` (0.25mm, `band.py`), an inline `0.02mm` fillet-attempt threshold (`band.py`), `_MIN_INNER_RADIUS_MM` (0.2mm, `basket.py`), and `FlatCircleAtRadius.tol` (1e-3mm, `geometry/primitives/selectors.py`). All 7 are now documented in [`07-atlas/124-geometric-primitives.md`](124-geometric-primitives.md), [`126-curve-and-profile-model.md`](126-curve-and-profile-model.md), [`135-fillets-rounding-and-fallbacks.md`](135-fillets-rounding-and-fallbacks.md), [`136-tolerance-model.md`](136-tolerance-model.md), and [`appendices/atlas-geometry-invariant-catalog.md`](../appendices/atlas-geometry-invariant-catalog.md). None was found to be a defect — no code was changed.

## Hidden jewelry-domain assumptions found

**0 new ones.** A specific, deliberate search confirmed geometry code does **not** enforce jewelry-domain thresholds anywhere: `build_prongs()` will construct any non-negative prong count — the `{4, 6}` restriction lives entirely in Forge's `JM-PRONG-001`, never in the geometry builder. The stone reference's crown/pavilion/table proportions are geometric-construction necessities already documented in Sprint 1's `docs/known-limitations.md`, not newly-hidden thresholds. This is a genuinely clean result for ATLAS-GOV-002, confirmed by inspection, not assumed.

## Fallbacks discovered

See "Geometry fallbacks discovered" above (2).

## Runtime geometry inspections currently implemented

**1**: the fuse-solid-count check inside `_fuse_metal()`, backing Sprint 4's `FORGE-GEOM-001`.

## Inspections existing only in tests

**7 check categories**: shape existence, positive volume, bounding-box plausibility, component count, generated-vs-requested prong count, stone-metal Z separation, and cross-profile volume differentiation — all verified only by `backend/tests/test_geometry.py`, never re-checked for a real user's specific input at runtime. This is the single most significant finding of Sprints 4 and 5 combined, restated and formalized here with the full Atlas inspection vocabulary.

## Component-separation risks

**None found beyond the already-known, already-mitigated one.** `_fuse_metal()`'s function signature structurally cannot accept a stone argument — stone-metal fusion is prevented by the shape of the code, not merely a runtime flag. The one residual gap: no check confirms *true geometric non-intersection* between stone and metal, only a bounding-box Z-separation check (sufficient for current geometry, not exhaustive for all hypothetical future geometry) — recorded as `ATLAS-GAP-006`.

## Code/documentation mismatches found

**0.** Every fact newly documented this Sprint was previously *absent* from the Bible and `docs/`, never *contradicted* by it. One internal Bible cross-reference error (a wrong section number in a Sprint 5 document's own link to a Sprint 3 document) was found and corrected during this Sprint's own link-validation pass, not a pre-existing mismatch between docs and code.

## Atlas gaps identified

**16**, cataloged in [`150-atlas-gap-analysis.md`](150-atlas-gap-analysis.md): runtime topology validation, runtime connectivity inspection, systematic boolean diagnostics, local-thickness analysis, general intersection analysis, stone-metal clearance analysis, support continuity verification, geometry plan abstraction, component graph, kernel-version recording, deterministic per-prong identity, a general fallback policy, geometry regression fixtures, STEP re-import validation, STL mesh validation, and performance benchmarks. Zero gaps require jewelry expertise on the Atlas side; where a gap's *result* needs domain interpretation, that step correctly belongs to Forge.

## A real cross-platform reproducibility gap, caught by CI

The first CI run of this Sprint's commit **failed** one new test: `test_atlas_registry.py::test_metadata_vectors_match_live_geometry_generation` asserted exact equality on `combined_metal_volume_mm3` (`341.44334316907685` on CI's Linux/OCCT build vs. `341.44334316909976` on the Windows machine the vector was generated on — a difference starting at the 9th significant digit). This is precisely the risk [`137-determinism-and-reproducibility.md`](137-determinism-and-reproducibility.md) (open question `ATLAS-OQ-009`) predicted and flagged as untested — CI just supplied the first real evidence that it occurs. **Fix applied**: the test now compares OCCT-kernel-derived floats (volumes, bounding boxes) with a relative tolerance (`pytest.approx(..., rel=1e-6)`) instead of exact equality, while keeping exact equality for pure-Python values (`definitionHash`, `generatorVersion`, `generatedProngCount`) that have no floating-point-kernel dependency. `specs/atlas/v1/test-vectors/metadata-vectors.json` was annotated with this finding. No geometry code, threshold, or checked-in vector value was changed — only the test's comparison strictness, to match what the Sprint's own documentation already said was and wasn't guaranteed.

## Tests passed

| Suite | Result |
|---|---|
| Backend full suite | **170 passed** (165 pre-existing + 5 new, `test_atlas_registry.py`) |
| Backend lint (`ruff check`) | Clean |
| Geometry + Atlas registry tests specifically | **19 passed** (`test_geometry.py` + `test_atlas_registry.py`) |
| API export/preview/generate tests specifically | **8 passed** (`test_api.py`, filtered) |
| Frontend test suite | **41 passed**, unchanged — no frontend code was modified this Sprint |
| Frontend type check (`tsc -b`) | Clean |
| Frontend production build (`vite build`) | Succeeds (pre-existing chunk-size warning only) |
| Atlas JSON Schemas valid (Draft 2020-12) | Yes, all 6 |
| Atlas examples pass their schemas | 5 / 5 |
| Atlas metadata/coordinate vectors match a live run | Confirmed (`test_atlas_registry.py`) |
| Markdown relative links across `docs/bible/` (171 files checked) | All resolve, after this report's own file was created and one internal link fix |
| Front matter completeness | Complete on all files |
| Duplicate Bible document IDs | None found |
| Personal email addresses / absolute local Windows paths | None found |

## What was, and was not, changed in application code

**Changed**: `backend/tests/test_atlas_registry.py` (new test file only). **Not changed**: no field, geometry builder, exporter, preview logic, or frontend component was modified. No magic number was altered — every one found was documented, not "fixed," since none was a defect. This Sprint is documentation- and specification-only, exactly as required.
