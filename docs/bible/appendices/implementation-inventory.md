---
id: JM-BIBLE-A01
title: "Appendix: Implementation Inventory"
version: 1.0.0
status: accepted
owner: JewelMind
last_updated: 2026-07-31
source_of_truth: true
depends_on: []
related_documents:
  - JM-BIBLE-021
implementation_status: current
---

# Appendix: Implementation Inventory

Generated by inspecting the actual modules in `backend/jewelmind/`,
`frontend/src/`, and `shared/`.

| Module | Responsibility | Maturity | Public interface | Dependencies | Limitations |
|---|---|---|---|---|---|
| `domain/schema.py` | Canonical `JewelryDefinition` and nested models. | Mature — hardened for strict types, finite numbers, locked schema version. | `JewelryDefinition`, `ProjectInfo`, `RingSpec`, etc. | Pydantic v2. | Hand-mirrored to TypeScript; no codegen. |
| `domain/defaults.py` | The default definition. | Mature. | `default_definition()` | `domain/schema.py`. | None known. |
| `domain/disclaimer.py` | Canonical disclaimer text. | Mature. | `PROFESSIONAL_REVIEW_NOTICE` | None. | Must be kept identical to `shared/disclaimer.ts` by hand. |
| `validation/engine.py` | Runs the thirty-eight business rules (16 through Sprint 4, +2 bezel in Sprint 19, +6 gem in Sprint 21, +6 arrangement in Sprint 22, +3 advanced setting in Sprint 23, +5 multi-stone family in Sprint 24). | Mature. | `validate_definition()`, `has_errors()` | `validation/rules.py`, `validation/sizing.py`. | Mirrored by hand in `shared/validation/engine.ts`. |
| `validation/sizing.py` | EU/French size ↔ diameter conversion. | Mature. | `eu_size_to_inner_diameter()`, `inner_diameter_to_eu_size()`, `sizing_consistency()` | None. | EU/French convention only, not German. |
| `geometry/constants.py` | Coordinate convention constants. | Mature. | `inner_radius()`, `outer_radius()`, `band_top_z()`, `prong_center_radius()`, `EMBED_MM` | None. | None known. |
| `geometry/model.py` | Result types for geometry builders. | Mature. | `BoundingBox`, `GeneratedComponent`, `GeneratedModel` | CadQuery. | None known. |
| `geometry/components/band.py` | Thin re-export of `geometry/shank/build_shank()` (Sprint 17). | Mature. | `build_ring_band()` | `geometry/shank/`. | See `geometry/shank/` rows below. |
| `geometry/shank/profile.py` | Section-profile builder (flat/comfort-fit), shared by uniform and tapered paths. | Mature — Sprint 17. | `build_profile()`, `flat_profile_wire()`, `comfort_fit_profile_wire()` | CadQuery. | Knife-edge profile PLANNED, not implemented. |
| `geometry/shank/taper.py` | Deterministic head-anchored taper interpolation. | Mature — Sprint 17. | `taper_ratio()`, `angle_deg_for_u()` | `domain/schema.py::BandTaperSpec`. | Only `TOWARD_BOTTOM`, linear; `TOWARD_HEAD` PLANNED. |
| `geometry/shank/builder.py` | Dispatches uniform revolve vs 48-section tapered loft. | Mature — Sprint 17, 77 dedicated tests. | `build_shank()` | CadQuery, `geometry/connection.py`, `geometry/shank/{profile,taper}.py`. | Tapered path has no outer-rim fillet (documented v1 limitation). |
| `geometry/shank/capability.py` | CURRENT vs PLANNED Shank capability registry. | Mature — Sprint 17. | `SHANK_CAPABILITIES`, `get_shank_capability()` | None. | Mirrored at `specs/shank/v1/capability-registry.json`. |
| `geometry/connection.py` | Explicit Shank → RingHead connection interface. | Mature — Sprint 17. | `ShankConnectionInterface`, `shank_connection_interface()` | `geometry/constants.py`. | None known. |
| `geometry/components/stone.py` | Thin re-export of `geometry/stone/build_stone()` (Sprint 18). | Mature. | `build_stone_reference()` | `geometry/stone/`. | See `geometry/stone/` rows below. |
| `geometry/stone/outline.py` | 7 pure 2D outline primitives → closed `cq.Wire`. | Mature — Sprint 18. | `round_outline()`, `oval_outline()`, `marquise_outline()`, `pear_outline()`, `emerald_outline()`, `princess_outline()`, `cushion_outline()` | CadQuery. | Software reference outlines, not commercial cuts. Pear is a simplified non-tangent silhouette. |
| `geometry/stone/builder.py` | Dispatches round (byte-identical pre-Sprint-18 path) vs a shared 3-level loft for 6 shapes; applies orientation. | Mature — Sprint 18, 92 dedicated tests. | `build_stone()` | CadQuery, `geometry/constants.py`, `domain/stone_dimensions.py`, `geometry/stone/{outline,errors}.py`. | No faceting; `isGemologicalReproduction` always `false`. |
| `geometry/stone/capability.py` | CURRENT vs PLANNED shape registry; setting-compatibility axis. | Mature — Sprint 18. | `STONE_SHAPE_CAPABILITIES`, `get_stone_shape_capability()` | `domain/schema.py`. | Mirrored at `specs/stone/v1/shape-registry.json`. |
| `geometry/stone/errors.py` | Structured Stone System errors. | Mature — Sprint 18. | `StoneShapeUnsupportedError`, `StoneGenerationError` | None. | None known. |
| `geometry/stone/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 18. | — | None. | Prevents a real circular import; see the file's own docstring. |
| `domain/stone_dimensions.py` | The single LENGTH/WIDTH/DEPTH resolution point, consumed by both Atlas and Forge. | Mature — Sprint 18. | `resolved_length_mm()`, `resolved_width_mm()`, `resolved_depth_mm()` | `domain/schema.py`. | In `domain/` so neither Atlas nor Forge gains a new dependency on the other. |
| `geometry/components/prongs.py` | Prong builder. | Mature. | `build_prongs()` | CadQuery, `geometry/connection.py` (Sprint 17). | Plain cylinders, 4 or 6 count only. |
| `geometry/components/basket.py` | Basket support builder. | Mature — deliberately simple. | `build_basket_support()` | CadQuery, `geometry/connection.py` (Sprint 17). | Plain cylindrical shell, not decorative. |
| `geometry/assemblies/solitaire.py` | Combines all four components. | Mature — 6 dedicated tests covering the full assembly. | `build_solitaire_ring()` | All `geometry/components/*`. | Falls back to a multi-solid compound if boolean fuse fails (by design, LAW-005). |
| `preview/mesh.py` | Tessellates components to STL + manifest. | Mature. | `write_component_previews()` | `geometry/model.py`. | Per-component STL, not a single packaged GLB (ADR-007). |
| `exporters/filenames.py` | Filename sanitization. | Mature. | `sanitize_filename()` | None. | None known. |
| `exporters/step_exporter.py` | Real STEP export. | Mature. | `export_step()` | CadQuery. | Excludes stone by default. |
| `exporters/stl_exporter.py` | Real STL export. | Mature. | `export_stl()` | CadQuery. | Excludes stone by default. |
| `exporters/json_exporter.py` | Canonical JSON export. | Mature. | `export_json()` | `domain/schema.py`. | None known. |
| `exporters/specification.py` | Markdown specification. | Mature — timestamp bug fixed in the hardening pass. | `build_specification()` | `domain/disclaimer.py`, `geometry/model.py`, `validation/rules.py`. | Markdown only, no PDF. |
| `services/model_service.py` | Orchestration, cache, temp-file lifecycle. | Mature — hardened for unique temp files and cleanup. | `model_service` (singleton), `ModelService`, `ModelRecord` | All of the above. | In-memory cache, capped at 20 entries, cleared on restart. |
| `services/cad_engine.py` | One-time CAD engine readiness probe. | Mature — added in the hardening pass. | `probe_cad_engine()`, `cad_engine_ready()`, `cad_engine_error()` | CadQuery. | Probes once at import time; does not re-check mid-process (by design — see ADR discussion in `004-jewelmind-constitution.md` context). |
| `api/app.py` | FastAPI app factory. | Mature — hardened for safe error-detail serialization. | `create_app()`, `app` | FastAPI, `api/errors.py`, `api/routes.py`. | None known. |
| `api/routes.py` | All HTTP routes. | Mature — hardened for lazy CAD import and error-code mapping. | Nine routes (see `appendices/api-inventory.md`). | `services/model_service.py` (lazily), `services/cad_engine.py`, `validation/engine.py`. | None known. |
| `api/schemas.py` | Request/response models. | Mature — hardened for strict types and tolerance validation. | `HealthResponse`, `GenerateResponse`, `ExportStlRequest`, etc. | Pydantic v2, `validation/rules.py`. | None known. |
| `api/errors.py` | Error type hierarchy. | Mature. | `AppError` and its subclasses. | None. | None known. |
| `utils/hashing.py` | Canonical serialization + hashing. | Mature. | `canonical_json()`, `definition_hash()` | `domain/schema.py`. | None known. |
| `utils/logging.py` | Structured logging config. | Mature. | `configure_logging()`, `get_logger()` | `structlog`. | None known. |
| `frontend/src/store/useProjectStore.ts` | The single Zustand store. | Mature. | `useProjectStore` and its actions. | Zustand, `api/client.ts`, `store/persistence.ts`. | None known. |
| `frontend/src/store/persistence.ts` | `localStorage` load/save/clear. | Mature — hardened with structural validation. | `loadDefinition()`, `saveDefinition()`, `clearDefinition()` | `shared/types/jewelry-definition.ts::isValidJewelryDefinition`. | Single project slot. |
| `frontend/src/hooks/useComponentGeometries.ts` | Fetches/parses/disposes preview meshes. | Mature — hardened for abort/dispose/keep-last-good. | `useComponentGeometries()` | `three`'s `STLLoader`, `api/client.ts`. | None known. |
| `shared/types/jewelry-definition.ts` | TypeScript schema mirror + runtime guard. | Mature. | `JewelryDefinition`, `createDefaultDefinition()`, `isValidJewelryDefinition()` | None. | Hand-mirrored from the Python schema. |
| `shared/validation/engine.ts` | Frontend validation mirror. | Mature. | `validateDefinition()`, `hasErrors()` | `shared/validation/rules.ts`, `sizing.ts`. | Hand-mirrored from the Python engine. |
| `conversation/schemas.py` | Conversation data shapes (turn, session, proposal, clarification, diagnostics). | Mature — Sprint 12. | `ConversationSession`, `ConversationTurn`, `ConversationProposal`, `ClarificationThread`, `ConversationTurnRequest`, `ConversationResult` | Pydantic v2, `design_intent/schemas.py`, `designer/schemas.py`, `domain/schema.py`. | Carries no `JewelryDefinition`/`DesignIntent` copy, only content hashes (CONV-GOV-001/003). |
| `conversation/actions.py` | Deterministic turn classification into one of 13 `ConversationActionType` values. | Mature — Sprint 12. | `classify_action()` | `conversation/references.py`, `conversation/schemas.py`. | `ADD_INTENT`/`REMOVE_INTENT` are schema-complete but never returned — see `appendices/conversation-action-catalog.md`. |
| `conversation/references.py` | Deterministic reference resolution (explicit/implicit target, preserve phrases, material words). | Mature — Sprint 12. | `find_explicit_target()`, `find_preserve_target()`, `resolve_implicit_target()` | `design_intent/vocabulary.py::TARGET_SYNONYMS`. | Reuses Design Intent's own target vocabulary rather than duplicating a second word list. |
| `conversation/clarifications.py` | Clarification thread lifecycle (open/answer/cancel/supersede). | Mature — Sprint 12. | `open_clarification()`, `try_resolve_answer()`, `close_answered()`, `cancel()`, `supersede()` | `conversation/schemas.py`. | 4 `ExpectedAnswerType` values (`NUMERIC`, `ENUM_CHOICE`, `FREE_TEXT`, `CONFIRMATION`) — see `appendices/clarification-type-catalog.md`. |
| `conversation/state.py` | Session/proposal hashing, creation, and staleness detection. | Mature — Sprint 12. | `new_session()`, `refresh_hashes()`, `is_proposal_stale()`, `make_proposal()`, `intent_hash()` | `domain/schema.py`, `design_intent/schemas.py`, `utils/hashing.py::definition_hash()`. | Recomputes hashes fresh from the caller's own values on every request — never trusts a prior turn's cached hash. |
| `conversation/context.py` | Turn context construction and deterministic history summarization. | Mature — Sprint 12. | `build_turn_context()`, `compact_summary()`, `recent_turns()`, `MAX_RECENT_TURNS_IN_CONTEXT` (= 6) | `conversation/schemas.py`. | A turn-count bound, deliberately not an invented token budget. |
| `conversation/service.py` | Conversation orchestration around `DesignerService`. | Mature — Sprint 12. | `ConversationEngine`, `ConversationEngine.process_turn()` | `conversation/{actions,clarifications,references,state}.py`, `designer/service.py`, `designer/normalizer.py`. | Stateless per request, exactly like Designer — never persists a `ConversationSession` server-side. |
| `conversation/errors.py` | Conversation error/diagnostic codes. | Mature — Sprint 12. | `ConversationInvalidStateError` and 6 sibling `AppError` subclasses, `ALL_CONVERSATION_ERROR_CODES` | `api/errors.py::AppError`. | 4 of 7 `AppError` subclasses are currently unreachable — see `appendices/conversation-diagnostic-catalog.md`. Security screening reuses `DesignerSecurityRejectedError` directly rather than adding an 11th code. |
| `frontend/src/components/ConversationPanel.tsx` | Multi-turn conversational Studio UI, superseding `DesignerPanel` in `App.tsx`. | Mature — Sprint 12. | `ConversationPanel` (React component) | `api/client.ts`, `store/useConversationStore.ts`, `store/useProjectStore.ts`, `store/useDesignIntentStore.ts`. | `DesignerPanel.tsx` remains in the codebase, tested standalone, but is no longer the component `App.tsx` mounts. |
| `frontend/src/store/useConversationStore.ts` | Zustand store holding the current `ConversationSession`. | Mature — Sprint 12. | `useConversationStore`, `setSession()`, `resetSession()` | Zustand. | Holds only the session object — never a `JewelryDefinition`/`DesignIntent` copy. |
| `gem/models.py` | Gem vocabularies and models: `GemDefinition` (type level), `GemIdentity` (instance level), `GemTreatment`, `GemVisualProfile`, `ResolvedGem`, `StoneInstance`. | Mature — Sprint 21. | The models above plus `CUSTOM_GEM_ID`, `UNKNOWN_GEM_ID`, `GEM_ID_PATTERN`, `MAX_GEM_ID_LENGTH` | Pydantic v2 only. | Imports no jewelry category, no geometry and not `JewelryDefinition` — enforced by AST inspection in `test_gem_no_category_dependency.py`. |
| `gem/registry.py` | The 40-entry gem taxonomy and its alias index. | Mature — Sprint 21. | `GEM_REGISTRY`, `GEM_REGISTRY_VERSION`, `get_gem()`, `all_gem_ids()`, `current_gem_ids()`, `alias_lookup()`, `registry_families()` | `gem/models.py`. | An internal taxonomy, not a gemological database: no hardness, durability, heat-sensitivity or setting-suitability data. Every entry is `NOT_REVIEWED`. Mirrored at `specs/gem/v1/gem-registry.json`. |
| `gem/visual.py` | 20 visual profiles plus the neutral fallback. | Mature — Sprint 21. | `GEM_VISUAL_PROFILES`, `FALLBACK_PROFILE`, `VISUAL_PROFILE_SET_VERSION`, `get_visual_profile()`, `profile_exists()` | `gem/models.py`. | Every value is a RENDERING parameter, never an optical measurement. `get_visual_profile()` never raises. Mirrored in `frontend/src/vision/gemMaterials.ts`. |
| `gem/resolution.py` | The single join point: identity + entry + profile → `ResolvedGem`. | Mature — Sprint 21. | `resolve_gem()`, `resolve_alias()`, `is_valid_gem_id()`, `require_gem_id()`, `get_gem_or_raise()`, `require_origin_applicable()`, `effective_display_name()`, `treatment_summary()`, `GEM_SYSTEM_VERSION` | `gem/{models,registry,visual,errors}.py`. | `resolve_gem()` never raises and never substitutes a different real gem; an unresolvable reference degrades to `unknown` with `wasUnresolved` set and the original reference preserved. |
| `gem/errors.py` | Structured Gem System errors. | Mature — Sprint 21. | `GemError` plus `GemIdInvalidError`, `GemNotFoundError`, `GemOriginInvalidError`, `GemIdentityIncoherentError`, `GemTreatmentInvalidError`, `GemVisualProfileNotFoundError` | None. | None known. |
| `gem/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 21. | — | None. | Load-bearing: `domain/schema.py` imports `gem.models`, so an eager init would make the graph cyclic. Same trap as `stone/__init__.py`. |
| `designer/gem_language.py` | Deterministic gem/origin/treatment term recognition for Designer and Conversation. | Mature — Sprint 21. | `normalize_gem_term()`, `normalize_origin_term()`, `normalize_treatment_term()`, `gem_alias_index()`, `AMBIGUOUS_GEM_TERMS`, `ambiguity_reason()`, `unresolved_gem_reason()` | `gem/{models,registry}.py`. | Gem names are DERIVED from the live registry, never a second table. A term naming both a cut and a species (`emerald`, `pearl`) is reported ambiguous rather than resolved. |
| `utils/hashing.py::geometry_hash` | The geometry-scoped hash, excluding the fields verified not to affect geometry. | Mature — Sprint 21. | `geometry_hash()`, `geometry_canonical_json()` | `domain/schema.py`. | Every exclusion is an EMPIRICAL claim verified by generating geometry with that field varied. `definition_hash()` keeps its existing whole-document meaning. |
| `frontend/src/vision/gemMaterials.ts` | Gem-driven stone appearance in both view modes. | Mature — Sprint 21. | `resolveGemMaterial()`, `profileIdForGem()`, `isMirroredProfile()`, `mirroredProfileIds()`, `paramsFromBackendProfile()`, `FALLBACK_GEM_PROFILE_ID` | `shared/types/jewelry-definition.ts`. | A MIRROR of `gem/visual.py`, kept local so the viewer can draw before any network round trip; an unmirrored profile degrades to the neutral fallback rather than rendering as another gem. |
| `frontend/src/components/TextField.tsx` | Single free-text design-parameter control. | Mature — Sprint 21. | `TextField` (React component) | None. | The raw value is always passed through — JDL and Forge remain the authoritative validators, exactly as for `NumericField`. |
| `family/models.py` | Multi-stone family domain models: `FamilyDefinition`, `FamilyMember`, and the four discriminated parameter models. | Mature — Sprint 24. | The models above plus `FAMILY_ID_PATTERN`, `MAX_FAMILY_MEMBERS` | Pydantic v2, `arrangement/models.py`, `gem/models.py`. | Strict typing (JDL's own policy), carried directly in JDL. Reuses the arrangement's `InstanceTransform` for a placement override rather than declaring a second transform model. Imports no category, no geometry, no kernel. |
| `family/compile.py` | Family semantics -> arrangement primitives, one compiler per family. | Mature — Sprint 24. | `compile_family()`, `family_compilers()`, `family_types()`, `validate_roles()`, `FAMILY_ROLE_RULES` | `arrangement/models.py`, `family/{models,errors}.py`. | A registry, not a branch chain. The arrangement remains the placement authority; a symmetric three-stone emits a real MIRROR pattern so the engine does the mirroring. Rejects a malformed family rather than repairing it. |
| `family/effective.py` | The single point where a family and an explicit arrangement meet. | Mature — Sprint 24. | `effective_arrangement()` | `family/{compile,errors}.py`, `domain/schema.py` (typing only). | The only family module that touches a `JewelryDefinition` — the role `geometry/setting_adapter.py` plays for settings. Declaring both a family and an arrangement is refused, never merged. |
| `family/capability.py` | CURRENT/PARTIAL/PLANNED registry across four axes (representable / compilable / stoneGeometry / settingGeometry). | Mature — Sprint 24. | `FAMILY_CAPABILITIES`, `FAMILY_FEATURE_CAPABILITIES`, `RESERVED_FAMILY_TYPES`, `FAMILY_COMPILER_VERSION` | Pydantic v2 only. | Mirrored (never hand-copied) at `specs/family/v1/family-registry.json`. Every family is PARTIAL: stone geometry is real, an accent setting is not. |
| `family/errors.py` | Structured family errors. | Mature — Sprint 24. | `FamilyError` plus eight subclasses. | None. | Plain exceptions rather than `AppError` subclasses: the package is transport-neutral. |
| `family/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 24. | — | None. | Load-bearing: `domain/schema.py` imports `family.models`, so an eager init would make the graph cyclic. Same trap as `stone/`, `gem/` and `arrangement/`. |
| `halo/models.py` | Halo domain models: `HaloDefinition`, `HaloRing`, and the ring-cardinality table. | Mature — Sprint 25. | The models above plus `HALO_RING_CARDINALITY`, `MAX_HALO_RINGS`, `MAX_HALO_STONES` | Pydantic v2, `arrangement/models.py`, `family/models.py`, `gem/models.py`. | Strict typing (JDL's own policy), carried directly in JDL. REUSES `FamilyMember` for per-stone overrides and the arrangement's `InstanceTransform` for a placement override rather than declaring parallel models. A HIDDEN halo's negative offset is enforced structurally. Imports no category, no geometry, no kernel. |
| `halo/compile.py` | Halo semantics -> arrangement primitives, composed onto an existing arrangement. | Mature — Sprint 25. | `compose_halo()`, `halo_variants()` | `arrangement/{models,radial}.py`, `halo/{models,errors}.py`, `family/models.py`. | Composes, never replaces: `compose_halo(base, None)` returns `base` itself. The centre is resolved by id against the real instances and refused if absent. Rings are placed explicitly using the SHARED ring arithmetic, not through a RADIAL pattern (whose members would inherit the centre stone's scale). |
| `halo/capability.py` | CURRENT/PARTIAL/PLANNED registry across four axes (representable / composable / stoneGeometry / settingGeometry), plus the family-composition support table. | Mature — Sprint 25. | `HALO_CAPABILITIES`, `HALO_FEATURE_CAPABILITIES`, `HALO_COMPOSITION`, `RESERVED_HALO_VARIANTS`, `HALO_COMPILER_VERSION` | Pydantic v2 only. | Mirrored (never hand-copied) at `specs/halo/v1/halo-registry.json`. Every variant is PARTIAL: halo stone geometry is real, halo metal is not. `HALO_COMPOSITION` is REPORTING; the compiler is the gate, and a test re-derives every row by actually composing. |
| `halo/errors.py` | Structured halo errors. | Mature — Sprint 25. | `HaloError` plus five subclasses. | None. | Plain exceptions rather than `AppError` subclasses: the package is transport-neutral. |
| `halo/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 25. | — | None. | Load-bearing: `domain/schema.py` imports `halo.models`, so an eager init would make the graph cyclic. Same trap as `stone/`, `gem/`, `arrangement/` and `family/`. |
| `arrangement/radial.py` | The one place a ring of stones becomes a list of angles. | Mature — Sprint 25. | `ring_angles_deg()` | None. | EXTRACTED rather than added: the resolver and the family compiler each carried a copy of this arithmetic, and a third for the halo would have made drift a matter of time. Expression-for-expression identical to the original, so no existing resolution moved. |
| `pave/models.py` | Pavé domain models: `PaveDefinition`, `PaveSpec`, `MicrosettingSpec`, `PaveRetention`, `PaveSeat`, and the default field. | Mature — Sprint 26. | The models above plus `MAX_PAVE_STONES`, `DESIGN_CENTER_INSTANCE_ID`, `default_pave_field()` | Pydantic v2, `arrangement/models.py`, `gem/models.py`. | Strict typing (JDL's own policy), carried directly in JDL. Two first-class spec models rather than one with a flag: a PAVE states an area and a density, a MICROSETTING a structure. `default_pave_field()` is the single definition of 'turn the pavé on', shared by Designer and the Studio panel. Imports no category, no geometry, no kernel. |
| `pave/compile.py` | Pavé semantics -> arrangement primitives, plus the retention anchor lattice. | Mature — Sprint 26. | `compile_pave_field()`, `compose_pave()`, `lattice_pitches()`, `surface_lattices()`, `pave_patterns()` | `arrangement/{models,radial}.py`, `pave/{models,errors,surface}.py`. | Composes, never replaces. Places the lattice EXPLICITLY (a pattern's members would inherit the centre stone's scale) using the SHARED ring arithmetic. Retention anchors are derived in the surface's own parameters so adjacent cells share a corner exactly — in 3D they would not, and a shared-bead field would silently become an individual-bead one. |
| `pave/surface.py` | The host surface contract: what the neutral layer needs to know about a surface, as numbers. | Mature — Sprint 26. | `ResolvedHostSurface` | `pave/models.py`. | Two surface kinds and only two, because only two are executable. Carries `stoneAnchorZMm` because an arrangement transform is a DELTA, not an absolute position. |
| `pave/capability.py` | CURRENT/PARTIAL/PLANNED registry across four axes, plus reserved hosts and retention strategies. | Mature — Sprint 26. | `PAVE_CAPABILITIES`, `RESERVED_PAVE_HOSTS`, `RESERVED_RETENTION_STRATEGIES`, `PAVE_COMPILER_VERSION` | Pydantic v2 only. | Mirrored (never hand-copied) at `specs/pave/v1/pave-registry.json`. The first registry in the programme where `settingGeometry` is true, asserted comparatively against the Family and Halo registries. |
| `pave/errors.py` | Structured pavé errors. | Mature — Sprint 26. | `PaveError` plus seven subclasses. | None. | Plain exceptions rather than `AppError` subclasses: the package is transport-neutral. |
| `pave/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 26. | — | None. | Load-bearing: `domain/schema.py` imports `pave.models`. Same trap as `stone/`, `gem/`, `arrangement/`, `family/` and `halo/`. |
| `geometry/pave_surface.py` | JewelryDefinition -> resolved host surface. | Mature — Sprint 26. | `resolve_pave_surface()`, `stone_anchor_z_mm()` | `geometry/constants.py`, `domain/stone_dimensions.py`, `pave/`. | Deliberately KERNEL-FREE so Forge can import it: a pavé rule must resolve a surface with the same code generation uses, and Forge must not import CadQuery. Resolves from parameters, never from a face index. |
| `geometry/pave_adapter.py` | The kernel half: retention component and recess. | Mature — Sprint 26. | `retention_component()`, `apply_pave_recess()` | `setting/{retention,seat}.py`, `geometry/pave_surface.py`. | Contains no `.fuse` call at all — the recess routes through `setting/seat.py`, inheriting its never-fuse guarantee rather than restating it (AST-asserted). |
| `ring_family/models.py` | The ring-family taxonomy: variant ids, the family mapping, the JDL-carried spec model, the parameter contract, reserved names and identity. | Mature — Sprint 28. | `RingFamilyVariantId`, `RingFamilySpec`, `RingFamilyParams`, `RESERVED_RING_FAMILIES`, `VARIANT_HEAD_HEIGHT_FACTOR`, `default_variant_for()`, `ring_family_fingerprint()` | Pydantic v2, `ring_family/errors.py`. | KERNEL- AND CATEGORY-FREE, because it is carried in JDL and read by Forge. Carries NO `family` field: `jewelry.style` is the one authority, so a variant belonging to another family is refused rather than resolved. `default_variant_for()` raises a DOMAIN error rather than a `KeyError` — it shipped as the latter, and a caller guarding on `RingFamilyError` could not catch it. |
| `ring_family/dependencies.py` | The parametric dependency table: every path a variant derives, and every document field that influences what it derives. | Mature — Sprint 28. | `RING_FAMILY_DEPENDENCIES`, `derived_paths()`, `influenced_paths()`, `dependency_graph()` | Pydantic v2. | The parametric claim as INSPECTABLE DATA, so a report can state it without running the resolver — and the resolver self-checks every write against it at runtime, because a derivation no report could explain is worse than a missing one. |
| `ring_family/resolve.py` | The single resolution point: which family and variant a design uses, and every derivation it makes. | Mature — Sprint 28. | `resolve_ring_family()`, `ResolvedRingFamily`, `RingFamilyDerivation`, `parameters_read_by()`, `unread_parameters()`, `resolution_summary()` | `domain/schema.py` (under `TYPE_CHECKING`), `ring_family/{models,dependencies,errors}.py`. | ONE resolution point, the role `effective_arrangement()` and `resolve_primary_mode()` play in their own layers: a second consumer that re-derived the family would eventually disagree with the geometry. Every derivation records the parameters it was computed from — without that this is a system that happened to change a number. |
| `ring_family/capability.py` | The variant capability registry, with its central axis measured. | Mature — Sprint 28. | `ring_family_capabilities()`, `family_status()`, `designer_family_terms()`, `capability_summary()` | `ring_family/{models,resolve}.py`, `domain/schema.py` (lazily). | `differsFromFamilyBaseline` and `derivedPathCount` are MEASURED by running the real resolver. `structuralGeometry` is DECLARED, and that is a boundary rather than a gap: measuring it needs `jewelmind.geometry` and this layer must not import it, so the correspondence is asserted in the test file instead — the resolution `pave/capability.py` already uses. A family's status is the WEAKEST of its variants'. |
| `geometry/ring_family_adapter.py` | The sanctioned meeting point between the neutral layer and `JewelryDefinition`. | Mature — Sprint 28. | `effective_definition()`, `ring_family_summary()` | `domain/schema.py`, `ring_family/resolve.py`. | Returns the ORIGINAL document object when a family changes nothing, which is what keeps every pre-Sprint-28 design on the exact path it always took. "Nothing changed" is not "nothing derived": the baseline derives `setting.basketHeight` from the document's own value times two factors of 1.0, so identity is preserved by comparing each write's RESULT rather than by counting derivations. |
| `geometry/shank/architecture.py` | The split and bypass shank architectures. | Mature — Sprint 28. | `build_split_shank()`, `build_bypass_shank()`, `rail_half_width()`, `SHANK_ARCHITECTURE_BUILDERS` | CadQuery, `geometry/shank/{profile,taper}.py`, `geometry/constants.py`. | `SHANK_ARCHITECTURE_BUILDERS` has NO `UNIFORM` entry, deliberately: the uniform path is `_build_uniform_shank()`, reached BEFORE the dispatch, and keeping it out is what stops SHANK-GOV-003's byte-identity guarantee growing a wrapper. Both builders raise if they produce more than one solid. Revolve FIRST and translate the shape after — `Workplane.translate()` before `.revolve()` silently loses the offset, which shipped as coincident rails. |
| `geometry/shoulder.py` | Shoulder arches: the shank-to-head transition as real metal. | Mature — Sprint 28. | `build_shoulders()`, `MAX_ARCH_SPAN_DEG`, `SHOULDER_ARCHITECTURES`, `SHOULDER_COMPONENT` | CadQuery, `geometry/shank/{profile,taper,architecture}.py`, `geometry/connection.py`. | The base section IS the band's own profile wire, so a shoulder cannot disagree with the band about its cross-section. ONE `shoulders` component for every arch, not one per arch. `MAX_ARCH_SPAN_DEG = 90.0` is a MEASURED construction limit refused as a PRECONDITION — past a quarter turn the ruled loft self-intersects and OCC's own validity check passes it, so checking the result would not catch it. |
| `geometry/signet.py` | The signet body: a solid with a table at the ring's top. | Mature — Sprint 28. | `build_signet_body()`, `BODY_ARCHITECTURES`, `SIGNET_COMPONENT` | CadQuery, `geometry/constants.py`, `geometry/shank/taper.py`. | Its own component rather than metal fused anonymously into the band, so it has parametric identity. Embeds into the band so the fused body is one solid. The table is flat and rectangular and the component metadata says so, which is what stops a flat table being mistaken for a finished signet. |
| `setting/modes.py` | The setting-mode taxonomy: ids, axes, families, the JDL-carried spec model, resolution and identity. | Mature — Sprint 27. | `SettingModeId`, `SettingModeSpec`, `SettingModeParameters`, `RESERVED_SETTING_MODES`, `MODE_PARAMETER_FIELDS`, `resolve_primary_mode()`, `setting_mode_fingerprint()` | Pydantic v2, `setting/models.py`. | KERNEL-FREE, because it is carried in JDL and read by Forge — the same split `geometry/pave_surface.py` documents. Three axes preserved rather than collapsed: a primary and a head mode are simultaneous, not alternatives. `MODE_PARAMETER_FIELDS` is the authority for which mode reads which field, and a test asserts NO field is read by no mode. |
| `setting/frame.py` | The shared construction frame for the Sprint 27 families: a direction, extents along and across it, and a vertical anchor. | Mature — Sprint 27. | `axis_direction()`, `stone_extent_along()`, `crown_height_mm()`, `base_z()`, `oriented_prism()`, `fuse_all()` | CadQuery, `setting/{errors,models}.py`. | Contains NO constant at all, which is the simplest possible answer to 'does it invent a threshold?'. Extents are measured off the stone's real bounding box rather than its requested dimensions, because Sprint 20 found four shapes where those disagreed. A prism is rotated about its OWN centre before translation — rotating after would swing it round the design origin. |
| `setting/result.py` | Building one family's result record, including per-component provenance. | Mature — Sprint 27. | `family_result()`, `component_fact()` | `geometry/model.py`, `setting/models.py`. | Shared so provenance CANNOT be forgotten: the component list and the provenance list are derived from the same argument, so they cannot disagree in one direction only. |
| `setting/channel.py` | Channel setting: two parallel walls with optional end caps. | Mature — Sprint 27. | `generate_channel_setting()`, `CHANNEL_COMPONENT` | CadQuery, `setting/{frame,result,capability}.py`. | The document states the CLEAR distance between the walls, not a centre-to-centre one. A cap spans the full outer width so it genuinely meets both walls' material rather than touching them along a face. Records the arrangement instances it spans as opaque ids and never resolves them. |
| `setting/bar.py` | Bar setting: transverse bars across a run. | Mature — Sprint 27. | `generate_bar_setting()`, `BAR_COMPONENT` | CadQuery, `setting/{frame,result,capability}.py`. | ONE component for every bar, not one per bar — the reason `retention.py` documents at length: forty named components would give Inspection an intersection matrix growing with the bar count. Each bar's own offset is in the metadata instead. |
| `setting/flush.py` | Flush / gypsy setting: a solid collar with the stone cut out of it. | Mature — Sprint 27. | `generate_flush_setting()`, `FLUSH_COMPONENT` | CadQuery, `setting/{bezel,frame,result,stone_interface}.py`. | Reuses `bezel.py::offset_stone_outline()` rather than reimplementing it: a second offset would eventually miss the STEP-safety repair and ship a collar that re-imports as a zero-solid shell. Contains no `offset2D` call of its own (AST-asserted). The recess is the existing `REFERENCE_SEAT` relief, so relief is a PRECONDITION and the generator refuses without it. |
| `setting/tension.py` | Tension setting: two opposing supports. | Mature — Sprint 27. | `generate_tension_setting()`, `TENSION_COMPONENT`, `TENSION_SUPPORT_COUNT` | CadQuery, `setting/{frame,result,capability}.py`. | The support count is TWO and is not a parameter: 'tension' names a pair in opposition, and a count would let a document ask for one support that holds nothing. `padDepthMm` is a GEOMETRIC ROBUSTNESS overlap in the class of `constants.EMBED_MM`, explicitly not a grip depth. The component carries `structuralBehaviourModelled: false`, because the function is not modelled and the geometry must not imply it is. |
| `setting/retention.py` | Repeated small-stone retention metal: bead, shared bead, micro-prong, shared micro-prong. | Mature — Sprint 26. | `build_pave_retention()`, `retention_builders()`, `RetentionAnchorFrame`, `PAVE_RETENTION_COMPONENT` | CadQuery, `geometry/model.py`, `setting/{errors,models}.py`. | Lives in the SETTING SYSTEM because that system is authoritative for how metal holds a stone. ONE component for the whole field, not one per bead: sixty named components would give Inspection an intersection matrix growing with the stone count, and every anchor's id and the stones it serves are carried in the metadata instead. |
| `geometry/stone/instance.py` | Applies one resolved instance's transform, scale and orientation to a built stone. | Mature — Sprint 24. | `place_stone_instance()` | CadQuery, `geometry/model.py`. | Scale about the stone's own centre (the global origin would drop a raised stone) and rotation about its own axis (the design axis would swing it around the ring). An identity placement returns the input shape object unchanged. |
| `geometry/stone/placement.py` | One stone component per generated instance, named by the arrangement's contract. | Mature — Sprint 24. | `stone_components()`, `PRIMARY_STONE_COMPONENT` | `geometry/stone/instance.py`, `geometry/model.py`. | The stone is built once and placed many times, so two occurrences of one stone cannot differ in their last bits. No arrangement means one unplaced component. |
| `setting/head.py` | Category-neutral head geometry: basket, peg, martini, tulip. | Mature — Sprint 23. | `build_head()`, `head_builders()`, `head_architectures()`, `HEAD_COMPONENT` | CadQuery, `setting/{models,errors}.py`. | A registry, not a branch. `BASKET` reproduces the pre-Sprint-23 wall character-for-character; every architecture produces one connected solid over the same vertical extent, and the component is always named `basket_support`. `TRELLIS` has no builder and is not an enum member. |
| `setting/prong_styles.py` | Prong body geometry, one builder per style. | Mature — Sprint 23. | `build_prong_solid()`, `prong_solid_builders()`, `V_NOTCH_ANGLE_DEG`, `V_NOTCH_WIDTH_FACTOR` | CadQuery, `setting/{models,errors}.py`. | `ROUND_PRONG` is the pre-Sprint-23 cylinder, unchanged, and ignores `tipRatio` on purpose. The V notch is sized from the prong RADIUS, not its height — the first version cut the whole tip off and reported a V. |
| `setting/seat.py` | Reference seat relief: the stone cut out of production metal. | Mature — Sprint 23. | `apply_seat_relief()` | CadQuery, `setting/{models,errors}.py`, `geometry/model.py`. | A CUT, never a fuse, so LAW-006 holds — asserted by parsing this module's own source. Relief only: no bearing shoulder, and no claim a stone would sit correctly in it. Off by default. |
| `setting/dispatch.py` | Single setting entry point: family components, head, seat. | Mature — Sprint 19, extended Sprint 23. | `generate_setting()`, `setting_generators()` | `setting/*`, `geometry/model.py`. | The stone SHAPE is passed as an argument for seat relief and never stored on a domain model, so `SettingDefinition` stays kernel-neutral. A caller with no head and no seat gets exactly the pre-Sprint-23 result. |
| `geometry/components/basket.py` | Thin re-export of the Setting System's head builder (Sprint 23). | Mature. | `build_basket_support()` | `setting/head.py`, `geometry/setting_adapter.py`. | Head construction moved into the Setting System; this is the Ring-side entry point, the pattern `band.py`/`stone.py`/`prongs.py` already follow. Geometry unchanged for `BASKET`. |
| `arrangement/models.py` | Category-neutral arrangement domain models: `StoneInstanceDef`, `InstancePlacement`, `InstanceTransform`, `InstanceOverrides`, `ArrangementGroup`, the three pattern specs, `ArrangementRelation`, `ArrangementDefinition`, `ResolvedArrangement`. | Mature — Sprint 22. | The models above plus `ARRANGEMENT_ID_PATTERN`, `MAX_INSTANCES`, `MAX_COORDINATE_MM` | Pydantic v2 and `gem/models.py` only. | Strict typing (JDL's own policy) because these models are carried directly in JDL rather than through a `Jdl*` mirror. Imports no jewelry category, no geometry and not `JewelryDefinition` — enforced by AST inspection. |
| `arrangement/resolve.py` | Declarative arrangement -> explicit placements: pattern expansion, group-frame composition, reference validation. | Mature — Sprint 22. | `resolve_arrangement()` | `arrangement/{models,normalize,capability,errors}.py`. | One pass in a fixed order — no solver, no iteration, no random ids. Rejects a malformed structure rather than repairing it. Placement relative to another INSTANCE is PLANNED (it needs a dependency order). |
| `arrangement/normalize.py` | Canonical ordering, angle folding, coordinate quantization, canonical JSON and the arrangement fingerprint. | Mature — Sprint 22. | `normalize_definition()`, `canonical_json()`, `arrangement_fingerprint()`, `resolved_canonical_json()`, `normalize_angle_deg()`, `COORDINATE_DECIMALS` | `arrangement/{models,capability}.py`. | May reorder, round and wrap; may never change meaning. The fingerprint is separate from `definitionHash` and `geometryHash`, and the resolver version participates in it. |
| `arrangement/compile.py` | The arrangement -> compilation boundary: which resolved placements the current pipeline emits, and the stone component naming contract. | Mature — Sprint 22. | `compile_arrangement()`, `stone_component_name()`, `PRIMARY_STONE_COMPONENT`, `STONE_INSTANCE_COMPONENT_PREFIX` | `arrangement/{resolve,models,capability}.py`. | Kernel-free: decides names and statuses, never builds a shape. One stone component is emitted; additional instances are reported `NOT_GENERATED` with a reason. |
| `arrangement/capability.py` | CURRENT/PARTIAL/PLANNED registry across three independent axes (representable / resolvable / generatable). | Mature — Sprint 22. | `ARRANGEMENT_CAPABILITIES`, `RESOLVER_VERSION`, `current_capabilities()`, `generatable_capabilities()`, `resolvable_pattern_kinds()` | Pydantic v2 only. | Mirrored (never hand-copied) at `specs/arrangement/v1/arrangement-registry.json`. Exactly one capability is `generatable` today. |
| `arrangement/errors.py` | Structured arrangement errors. | Mature — Sprint 22. | `ArrangementError` plus eight subclasses. | None. | Plain exceptions rather than `AppError` subclasses: this package is transport-neutral. |
| `arrangement/__init__.py` | Deliberately **non-eager** — imports nothing. | Mature — Sprint 22. | — | None. | Load-bearing: `domain/schema.py` imports `arrangement.models`, so an eager init would make the graph cyclic. Same trap as `stone/` and `gem/`. |
| `geometry/roles.py` | Component role classification, now instance-aware. | Mature — Sprint 22 update. | `geometry_role()`, `production_role()`, `is_production_component()`, `GEOMETRY_ROLE`, `PRODUCTION_ROLE` | None. | The `stone_reference.<instanceId>` prefix resolves to `stone_reference`, NOT to the `production_metal` default — which would otherwise fuse an additional stone into metal and ship it in a production export. |
| `professional_validation/schemas.py` | Every Professional Validation Framework Pydantic model. | Mature — Sprint 13. | `ValidationRecord`, `ReviewerQualification`, `ValidationScope`, `ValidationEvidence`, `ValidationDecision`, `ReviewObservation`, `ReviewCase`, `ReviewSession`, `DisagreementRecord`, `ReviewPackageManifest` | pydantic. | `ValidationRecord.isTemplate` defaults `False` — a structural, not merely documentary, guard against a template entering the active registry. |
| `professional_validation/registry.py` | Loads and interprets the active validation registry. | Mature — Sprint 13. | `load_active_registry()`, `count_validated()`, `count_by_status()`, `validated_object_ids()` | `professional_validation/schemas.py`. | The real active registry currently contains zero records — `count_validated()` on it is always 0 today. |
| `professional_validation/scope.py` | Pure scope-matching logic. | Mature — Sprint 13. | `scope_matches()` | `professional_validation/schemas.py`. | An unset scope field never narrows a match — see PROVAL-GOV-015 through 018. |
| `professional_validation/versioning.py` | Classifies the impact of a version change on an existing validation record. | Mature — Sprint 13. | `classify_version_impact()` | None. | Pure version-string comparison — has no opinion on whether a specific change is actually safe. |
| `professional_validation/review_package.py` | Generates a real Professional Review Package ZIP from a generated model. | Mature — Sprint 13. | `build_review_package()` | `services/model_service.py`, `exporters/*`. | Not a replacement for Foundry's ordinary per-artifact exports — a separate, review-specific bundle. |
| `professional_validation/cli.py` | `validate-review-record` structural validator. | Mature — Sprint 13. | `validate_review_record_file()`, `validate_review_record_dict()`, `main()` | `professional_validation/schemas.py`. | Structural validation only — never judges whether professional feedback is correct. |
| `frontend/src/components/ProfessionalReviewPanel.tsx` | Studio's "Review" tab — generates a review package for the current model. | Mature — Sprint 13. | `ProfessionalReviewPanel` (React component) | `api/client.ts`, `store/useProjectStore.ts`, `studio/outputEligibility.ts`. | Gated by the same `computeOutputEligibility()` every other export tab uses — a stale/invalid model cannot produce a package. |

See [`appendices/test-inventory.md`](test-inventory.md) for what tests
exist per area, and
[`00-foundation/005-current-product-status.md`](../00-foundation/005-current-product-status.md)
for the capability-level (not module-level) summary.
