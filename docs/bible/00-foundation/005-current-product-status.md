---
id: JM-BIBLE-005
title: Current Product Status
version: 1.0.0
status: accepted
owner: JewelMind
last_updated: 2026-07-31
source_of_truth: true
depends_on:
  - JM-BIBLE-000
related_documents:
  - JM-BIBLE-001
  - JM-BIBLE-006
  - JM-BIBLE-A01
  - JM-BIBLE-A02
  - JM-BIBLE-DOMAIN-README
implementation_status: current
---

# Current Product Status

This is the factual implementation matrix for JewelMind. Nothing here is
marked `current` or `partial` without a file path and/or a test backing
it, per [`000-bible-governance.md`](000-bible-governance.md). Full detail
per capability lives in
[`appendices/implementation-inventory.md`](../appendices/implementation-inventory.md)
and [`appendices/test-inventory.md`](../appendices/test-inventory.md);
this table is the summary.

| Capability | Status | Relevant files | Relevant tests | Known limitations | Next improvement |
|---|---|---|---|---|---|
| `JewelryDefinition` schema | current | `backend/jewelmind/domain/schema.py`, `shared/types/jewelry-definition.ts` | `test_schema.py` (3), `test_schema_safety.py` (70) | Kept in sync by hand; no codegen between Python and TypeScript. | Codegen or a shared IDL if the schema grows. |
| Validation engine (16 rules) | current | `backend/jewelmind/validation/engine.py`, `shared/validation/engine.ts` | `test_validation.py` (20) | Frontend mirror must be updated by hand alongside the backend rule. | Generate the frontend mirror from the backend rule set. |
| Flat band geometry | current | `backend/jewelmind/geometry/shank/` (via `geometry/components/band.py` re-export) | `test_geometry.py::test_flat_band_is_valid_solid_with_positive_volume`, `test_shank.py` | Optional outer-rim fillet has a documented fallback path (`docs/known-limitations.md`); not applied when taper is requested. | None planned. |
| Comfort-fit band geometry | current | `backend/jewelmind/geometry/shank/` (via `geometry/components/band.py` re-export) | `test_geometry.py::test_comfort_fit_band_is_valid_solid_with_positive_volume`, `test_flat_and_comfort_fit_bands_differ_in_volume`, `test_shank.py` | Fixed flare constant, not user-adjustable. | Expose flare amount as a parameter if requested. |
| Band width/thickness taper (Sprint 17) | current | `backend/jewelmind/geometry/shank/taper.py`, `builder.py` | `test_shank.py` (77 tests) | Tapered shank does not apply the outer-rim fillet (v1 limitation). Only `TOWARD_BOTTOM`, linear. | `TOWARD_HEAD` or non-linear taper if requested — see [`19-shank/559-open-shank-questions.md`](../19-shank/559-open-shank-questions.md). |
| Stone reference geometry — 7 shapes (Sprint 18) | current (explicitly not gemological) | `backend/jewelmind/geometry/stone/`, `backend/jewelmind/domain/stone_dimensions.py` | `test_stone.py` (92), `test_stone_schemas.py` (18), `test_stone_system_no_ring_dependency.py` (8), `test_geometry.py::test_stone_reference_is_valid_and_separate_from_metal` | Simplified reference proportions, no faceting. Round + oval/pear/emerald/cushion/princess/marquise. Only `round` has `SUPPORTED` prong-setting compatibility; the other 6 are `EXPERIMENTAL`. See [LAW-006](004-jewelmind-constitution.md) and [`20-stone/README.md`](../20-stone/README.md). | Shape-optimized prong placement (tips/corners) — Sprint 23. |
| Prong setting — category-neutral (Sprint 19) | current | `backend/jewelmind/setting/` (`prong.py`, `placement.py`) | `test_setting.py` (102), `test_setting_system_no_ring_dependency.py` (13) | Cylindrical reference prongs, 4 or 6. `RADIAL` for round (byte-identical to pre-Sprint-19); `OUTLINE_CARDINAL` for non-round, which is a provisional software placement — it does not cluster prongs at marquise/pear tips or angular corners. No seat, bearing or cutter geometry. `NOT_REVIEWED`. See [`21-setting/README.md`](../21-setting/README.md). | `V_PRONG`/`CLAW` styles and shape-optimized placement — Sprint 23. |
| Bezel setting (Sprint 19) | current | `backend/jewelmind/setting/bezel.py` | `test_setting.py` (102), `test_setting_schemas.py` (22) | Real parametric wall offset from the stone's own girdle outline; all 7 stone shapes generate a valid single solid. Wall thickness/height are PRELIMINARY SOFTWARE VALUES, not professional recommendations. No seat/bearing/cutter. `NOT_REVIEWED`. The elliptical offset requires a documented STEP-safety resampling. | Seat geometry; professional review. |
| Setting capability + Stone × Setting matrix (Sprint 19) | current | `backend/jewelmind/setting/capability.py`, `specs/setting/v1/setting-registry.json` | `test_setting_schemas.py` (22) | `generatable` and `professionalValidationStatus` are independent axes; every family is `NOT_REVIEWED`. | Real professional review records. |
| Extended native stone cuts (Sprint 20) | current | `backend/jewelmind/geometry/stone/outline.py` | `test_stone_v2.py` (192) | 14 new cuts on top of Sprint 18's seven, for 21 total: heart, radiant, asscher, trillion, baguette, tapered_baguette, triangle, trapezoid, lozenge, hexagon, kite, shield, half_moon, pearl. Every one generates a real single solid with an exact STEP roundtrip and a bounding box equal to the request. Reference silhouettes only — no facet pattern is modelled, and every ratio is a software construction parameter. `NOT_REVIEWED`. See [`22-stone-v2/README.md`](../22-stone-v2/README.md). | Shape-optimized prong placement; professional review. |
| Stone reference profiles (Sprint 20) | current | `backend/jewelmind/geometry/stone/profile.py` | `test_stone_v2.py` (192) | A second axis independent of shape: `FACETED_REFERENCE` (all outlines), `CABOCHON_REFERENCE` (round, oval, heart, half_moon, custom), `SPHERICAL_REFERENCE` (pearl). This is why there is no `OVAL_CABOCHON` enum member. The cabochon dome is a 16-section ellipsoidal approximation chosen from a measured convergence run; no commercial cabochon proportion is claimed. | Near-round/drop/button pearl bodies. |
| Custom outline stones (Sprint 20) | current | `backend/jewelmind/stone/outline_validation.py`, `geometry/stone/outline.py::custom_outline` | `test_stone_v2.py` (192), `test_stone_v2_no_category_dependency.py` (29) | The escape hatch: any validated closed 2D outline becomes a real StoneReference through the same profile pipeline every native shape uses, and drives a real bezel with no per-shape code. Ordered points only; malformed outlines are rejected, never repaired. Concave outlines generate but are not bezel-verified for every concavity. | Curve segments and SVG import; an outline editor (Sprint 32). |
| Measured stones (Sprint 20) | current | `backend/jewelmind/stone/normalize.py` | `test_stone_v2.py` (192) | Real measurements of a real stone, with provenance preserved verbatim. A dimension-only result is labelled `MEASURED_DIMENSION_REFERENCE` and is explicitly NOT a model of the physical stone's surface. No measurement is ever invented. | Girdle measurements; a tolerance model. |
| Imported stone CAD/mesh (Sprint 20) | partial | `backend/jewelmind/stone/importing.py` | `test_stone_v2.py` (192) | STEP/BREP import yields a real B-Rep solid; STL yields a mesh with 0 solids and a null volume — the honest result. Units are declared, never guessed, and normalization is recorded. PARTIAL because setting compatibility is decided per asset from real geometry and outline projection does not exist, so imported stones are `UNSUPPORTED` for both setting families. | Outline projection; scan-specific processing. |
| Stone anchors (Sprint 20) | current | `backend/jewelmind/stone/anchors.py` | `test_stone_v2.py` (192) | Deterministic named points derived from the real outline: cardinal, tip, cleft, lobes, wide/narrow end, corners. Geometric facts only — never prong positions. An anchor a shape does not have is absent, never approximated. | Anchor-driven prong placement. |
| Multi-stone family semantics (Sprint 24) | current | `backend/jewelmind/family/`, `domain/schema.py::JewelryDefinition.family` | `test_multi_stone_families.py` (116), `familyValidation.test.ts` (15) | Four families — three-stone, toi-et-moi, cluster, centre-with-accents — as first-class SEMANTIC structure: roles, relationships, symmetry and per-member overrides. Category- and kernel-neutral, proven by AST inspection. A family REFERENCES the Stone and Gem systems and never restates them. See [`26-multi-stone-families/README.md`](../26-multi-stone-families/README.md). | Reserved families (halo/pave/eternity/bypass), each with a recorded reason. |
| Family compilation into arrangements (Sprint 24) | current | `backend/jewelmind/family/compile.py`, `family/effective.py` | `test_multi_stone_families.py` (116) | A family compiles into arrangement primitives, so the Stone Arrangement Engine remains the sole placement authority — a family cannot disagree with an arrangement about where a stone goes. Declaring both is refused (`JM-FAMILY-001`), never merged. Deterministic: repeated compilation is byte-identical and member order is irrelevant. | A family-aware head. |
| Per-instance stone geometry (Sprint 24) | current | `backend/jewelmind/geometry/stone/instance.py`, `stone/placement.py` | `test_multi_stone_families.py` (116), `test_arrangement.py` (105) | Every resolved instance builds a real stone solid, placed, scaled and oriented, named `stone_reference.<instanceId>`. Scale is applied about the stone's own centre and rotation about its own axis. An instance at the origin returns the builder's own shape object, so single-stone geometry is provably untouched. | Per-instance stone SPECIFICATIONS (only `primary` resolves). |
| Instance-aware geometry inspection (Sprint 24) | current | `backend/jewelmind/geometry/inspection/assembly.py` | `test_multi_stone_families.py` (116), `test_geometry_inspection.py` | The stone set is derived from the role map, every stone component is REQUIRED, and stone/metal separation is evaluated across all of them. A stone-to-stone overlap is reported as a pairwise geometric fact, never as a separation failure. | Interpreting an overlap (needs a professional threshold nobody here can source). |
| Multi-stone settings (Sprint 24) | partial | `backend/jewelmind/family/capability.py` | `test_multi_stone_families.py` (116) | A multi-stone family builds every stone and ONE setting: the primary stone's. Side and accent stones are placed, sized and rendered, and nothing holds them. Reported through `JM-FAMILY-005`, a note on the compiled arrangement, `settingGeometry: false` in the registry, and `settingCoverage: PRIMARY_ONLY` in every vector. Blocked on an accent-setting RFC that `SETTING-GOV` requires — see [`26-multi-stone-families/execution-boundary.md`](../26-multi-stone-families/execution-boundary.md). | An accent setting strategy (RFC), then a family-aware head. |
| Halo semantics (Sprint 25) | current | `backend/jewelmind/halo/`, `domain/schema.py::JewelryDefinition.halo` | `test_halo.py` (106), `haloValidation.test.ts` (15) | Single, double and hidden halos as a first-class COMPOSABLE structure: a halo adds rings of stones to whatever placement a design declares — a solitaire, a family or an explicit arrangement — rather than replacing it. Category- and kernel-neutral, proven by AST inspection. A halo REFERENCES the Stone and Gem systems, reuses the family's own member model and the arrangement's own transform, and never restates either. See [`27-halo/README.md`](../27-halo/README.md). | Reserved variants (cushion/floral/compass/triple/pave halo), each with a recorded reason. |
| Halo stone geometry (Sprint 25) | current | `backend/jewelmind/halo/compile.py`, `geometry/stone/instance.py` | `test_halo.py::TestGeometry`, `::TestHiddenHalo`, `goldens/` (49 cases) | Every halo stone is a real, individually identified solid with its own placement, scale, orientation and vertical offset. A hidden halo's offset is verified by measuring the built solid's bounding box, not by trusting the field. The fused metal body is unchanged by any number of halo stone references. | An outline-following halo (RFC); more than two rings. |
| Halo setting metal (Sprint 25) | partial | `backend/jewelmind/halo/capability.py` | `test_halo.py` (106) | A halo design builds every halo stone and ONE setting: the centre stone's. The halo stones are placed, sized, offset and rendered, and nothing holds them. Reported through `JM-HALO-005`, `settingGeometry: false` in the registry, `settingCoverage: PRIMARY_ONLY` in every vector, and `HALO_METAL_ABSENT` in every new Golden baseline. Blocked on the same setting RFC `SETTING-GOV` requires for accents — see [`27-halo/execution-boundary.md`](../27-halo/execution-boundary.md). | A halo setting strategy (RFC): a shared bezel rail, shared prongs, or cut-down bead setting. |
| Pavé & microsetting semantics (Sprint 26) | current | `backend/jewelmind/pave/`, `domain/schema.py::JewelryDefinition.pave` | `test_pave.py` (132), `paveValidation.test.ts` (14) | Two first-class field models: a PAVE states an area and a density, a MICROSETTING states a structure. Both compile into arrangement primitives on a resolved host surface. Category- and kernel-neutral, proven by AST inspection; a field REFERENCES the Stone, Gem, Arrangement and Setting systems and restates none of them. See [`28-pave/README.md`](../28-pave/README.md). | Reserved hosts and retention strategies, each with a recorded reason. |
| Pavé retention metal (Sprint 26) | current | `backend/jewelmind/setting/retention.py`, `geometry/pave_adapter.py` | `test_pave.py::TestRetention`, `::TestGeometry`, `goldens/` (54 cases) | THE FIRST REAL SETTING GEOMETRY IN THIS PROGRAMME. Beads, shared beads and micro-prongs are real solids fused into the production body; a shared bead names every stone it touches. An optional recess cuts the stones' own volume out of the host, routed through `setting/seat.py` so it inherits that module's never-fuse guarantee. | A real cut seat with a bearing shoulder (RFC); shared prongs and channels (RFC). |
| Pavé surface targeting (Sprint 26) | partial | `backend/jewelmind/geometry/pave_surface.py` | `test_pave.py::TestSurfaceTargeting` | Two hosts resolve: BAND_OUTER (cylindrical) and HEAD_PLANE (planar), both derived from the same `constants.py` expressions the assembly uses — never picked out of a solid, because a face index is not reproducible. Five targets are reserved with stated reasons, including HALO_PLANE (a halo has no metal to hold beads). | Setting/band-side/custom surfaces (RFC); more than one field per design. |
| Ring families as a parametric system (Sprint 28) | current | `backend/jewelmind/ring_family/` (`models`, `dependencies`, `resolve`, `capability`), `geometry/ring_family_adapter.py` | `test_ring_families.py` (125), `ringFamilyValidation.test.ts` (10), `RingFamilySection.test.tsx` (14) | Six families and thirteen variants in which a family is a PARAMETRIC RELATION, not a preset: changing `ring.innerDiameter`, `stone.diameter` or `band.width` regenerates the design. Every variant's difference from its family's baseline is MEASURED by running the real resolver, and exactly one variant may report no difference — `SOLITAIRE_CLASSIC`, which IS the baseline. The layer DERIVES JDL and orchestrates the Stone Arrangement Engine, the Halo System, the Pave Engine, Setting System v2 and the Shank subsystem; it computes no placement, outline or solid of its own. `jewelry.style` stays the one family authority and a variant belonging to another family is refused, never resolved by precedence. See [`30-ring-families/README.md`](../30-ring-families/README.md) and [`ADR-014`](../03-decisions/ADR-014-ring-family-orchestration.md). | Nine reserved families/variants, each with a recorded technical reason. Three wait on the same thing: a verified swept solid along a 3D spline. |
| Shoulder geometry (Sprint 28) | current | `backend/jewelmind/geometry/shoulder.py` | `test_ring_families.py::TestVariantGeometryDiffers`, `goldens/RF-001`, `goldens/RF-003` | The FIRST geometry the shoulder has ever had: `ring/models.py::ShoulderDefinition` recorded `modeled: False` from Sprint 16 until this sprint. Two arches for a cathedral, four for a split shank, each lofted from the band's OWN profile wire up to the head's OWN height, so a shoulder can never disagree with the band about its cross-section. One connected solid. Bounded at a MEASURED 90-degree construction limit — past a quarter turn a ruled loft self-intersects while OCC's own validity check passes it. | Sculpted and trellis shoulders, which need a swept solid along a 3D spline (RFC). |
| Split shank (Sprint 28) | current | `backend/jewelmind/geometry/shank/architecture.py` | `test_ring_families.py`, `test_shank.py::TestShankCapabilityRegistry`, `goldens/RF-003`, `goldens/RF-004` | TWO rails sharing the band's width, joined into one band over the bottom span and genuinely separated at the top — the axial gap contains no metal near the head and 9.778 mm3 at the bottom bridge. ONE connected solid. The rails SHARE the width, so a wider separation NARROWS them rather than widening the ring, and `JM-RINGFAM-004` refuses a separation that leaves no rail — arithmetic, not a judgment about thinness. The tapered variant uses the Shank subsystem's own `taper_ratio()` rather than a second narrowing mechanism. | More than two rails, which needs its own axial layout and bridge (RFC). |
| Bypass shank (Sprint 28) | current | `backend/jewelmind/geometry/shank/architecture.py` | `test_ring_families.py`, `goldens/RF-005` | ONE open rail travelling past a full turn with its axial position shifting as it goes, so its two ends occupy the same angles at different axial positions and PASS each other. Deliberately one rail rather than two arcs: two axially separated arcs come out as two disconnected solids, and a ring that is not one connected body is not a ring. | A twisting bypass, which needs a per-section rotation about the rail's tangent (RFC). |
| Signet body (Sprint 28) | partial | `backend/jewelmind/geometry/signet.py` | `test_ring_families.py`, `goldens/RF-006` | A real solid body with a rectangular table at the ring's top, built as its own `signet_body` component so it has parametric identity rather than being fused anonymously into the band. PARTIAL because a signet's DECORATION is the point of one and no engraving, relief or texture exists anywhere in the pipeline: the table is flat and rectangular. | Engraving and non-rectangular tables, both reserved with their real reasons (RFC). |
| Extended setting-mode taxonomy (Sprint 27) | current | `backend/jewelmind/setting/modes.py`, `setting/capability.py::setting_modes()` | `test_extended_setting_modes.py` (241), `settingModeValidation.test.ts` (14), `SettingModeSection.test.tsx` (10) | Twenty setting modes across three simultaneous axes — PRIMARY (how the design's own stone is held), HEAD (what the setting rises from) and RETENTION (how a field of small stones is held). Each row's `settingGeometry` axis is MEASURED from the live builder registries rather than declared, so a mode cannot claim a solid no builder produces. `setting.mode` REFINES `setting.type` and a disagreement is refused rather than resolved by precedence. See [`29-extended-setting-modes/README.md`](../29-extended-setting-modes/README.md) and [`ADR-013`](../03-decisions/ADR-013-extended-setting-modes.md). | Fifteen reserved modes, each with a recorded technical reason. |
| Channel setting (Sprint 27) | current | `backend/jewelmind/setting/channel.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-001` | Two parallel walls with the stones between them, running along a stated axis, with optional end caps. Extents default to the stone's own MEASURED box, so the single-stone case needs no parameters. The stones are the Stone Arrangement Engine's: this family builds the walls and records which instances they hold, never where those instances are. | Per-instance settings for a spanned row (RFC); shared-wall and tapered channels (RFC). |
| Bar setting (Sprint 27) | current | `backend/jewelmind/setting/bar.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-002` | Transverse bars across a run, one component carrying every bar with each bar's own offset in its metadata. Symmetric or asymmetric distribution is a real geometric difference. Retention only: no bar position is derived from a stone position or the reverse. | Tapered bars (RFC). |
| Flush / gypsy setting (Sprint 27) | current | `backend/jewelmind/setting/flush.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-003` | A solid collar built by reusing the BEZEL's verified outline offset, with the stone's own solid cut out of it through the existing `REFERENCE_SEAT` relief. Relief is a PRECONDITION rather than an option: without it the collar buries the stone, so the generator refuses and `JM-SETTING-010` reports it. The rim must stay below the stone's measured crown height. | Millgrain edges (RFC). |
| Tension setting (Sprint 27) | partial | `backend/jewelmind/setting/tension.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-006` | Two opposing supports with the stone between them, each reaching inward past its edge so the relief leaves a real groove. PARTIAL with COMPLETE geometry: a tension setting's whole FUNCTION is structural, and nothing here computes the elastic response of the metal, the force applied, or whether the stone would be retained. `professionalReviewRequirement` is REQUIRED, the component metadata carries `structuralBehaviourModelled: false`, and Geometry Inspection reports that as a fact. | A structural model, which needs engineering AND professional evidence. |
| Partial bezel (Sprint 27) | current | `backend/jewelmind/setting/bezel.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-004` | The FULL bezel's own wall with evenly spaced angular openings cut through it, so a partial bezel can never be a different wall from the full one it derives from. Legitimately several solids — n openings leave n arcs — and that count is reported rather than repaired. | Open-back and millgrain bezels (RFC). |
| Open gallery head (Sprint 27) | current | `backend/jewelmind/setting/head.py` | `test_extended_setting_modes.py::TestGeometry`, `goldens/ESM-005` | The basket wall with evenly spaced windows pierced through it, cut from the basket's own solid. Windows span only the middle fraction of the height so a continuous rim survives and the head stays ONE connected body. Deliberately NOT called azure: azure work is arbitrary decorative piercing, and this is the parametric subset of it. | Azure piercing and trellis heads (RFC). |
| Shared micro-prong retention (Sprint 27) | current | `backend/jewelmind/setting/retention.py` | `test_pave.py::TestCapabilityRegistry`, `goldens/ESM-007` | A micro prong at each SHARED lattice corner, serving every stone that touches it. Sprint 23 reserved this assuming it needed a distinct body; what it actually needed was the shared-corner topology the pavé lattice already computed, so it reaches the same builder `MICRO_PRONG` does — exactly as `BEAD` and `SHARED_BEAD` already share one. `ESM-007` records 46 prongs for 22 stones where an individual field would need 88. | — |
| Shared prong geometry (Sprint 27) | partial | `backend/jewelmind/setting/prong.py` | `test_extended_setting_modes.py::TestModeRegistry` | A prong serving two or more stones, expressed through EXPLICIT positions carrying `servesStoneInstanceIds`. The prongs are real solids and the assignment is reported per component. PARTIAL because the shared POSITION is stated by the document rather than derived from the two stones' own geometry — whether a shared prong reaches both stones it names is a geometric fact for inspection rather than a guarantee. This narrows, rather than repeats, Sprint 23's record. | Anchor-driven placement (RFC), then a Studio/Designer surface for it. |
| Setting component provenance (Sprint 27) | current | `backend/jewelmind/setting/{models,result}.py` | `test_extended_setting_modes.py::TestInspection::test_every_generated_component_carries_provenance` | Every generated setting component names the mode, the source stone, the arrangement instances it holds, and whether it is production metal or reference geometry. Recorded on the setting result rather than on a `GeometryPlan`, which remains deliberately unmaterialized: a fact about a component lives where the component's other facts live. | Materializing `GeometryPlan` (ADR). |
| Instance axis tilt (Sprint 26) | current | `backend/jewelmind/arrangement/models.py`, `geometry/stone/instance.py` | `test_pave.py::TestBackwardCompatibility::test_a_tilt_is_a_real_kernel_rotation` | An arrangement instance's axis can be tilted away from vertical in a stated direction, which together with its own spin is a complete ZXZ orientation. Sprint 22 recorded this PLANNED because no builder could execute it; Sprint 26 built one, because a pavé on a curved shank needs its stones to follow the surface normal. See [`ADR-011`](../03-decisions/ADR-011-instance-axis-tilt.md). | — |
| Prong style variants (Sprint 23) | current | `backend/jewelmind/setting/prong_styles.py` | `test_setting_v2.py` (96), `settingV2Validation.test.ts` (13) | 4 registry-dispatched styles: `ROUND_PRONG` (the pre-Sprint-23 cylinder, preserved character-for-character and still the default), `TAPERED_PRONG`, `CLAW_PRONG`, `V_PRONG`. Each builds one valid solid spanning the requested height. Notch angle, width factor and tip ratios are SOFTWARE CONSTRUCTION parameters; nothing is professionally validated. See [`25-setting-v2/README.md`](../25-setting-v2/README.md). | Anchor-driven placement; custom swept profiles. |
| Head architectures (Sprint 23) | current | `backend/jewelmind/setting/head.py` | `test_setting_v2.py` (96) | 4 architectures: `BASKET` (preserved byte-identically), `PEG_HEAD`, `MARTINI`, `TULIP`. Head construction is now CATEGORY-NEUTRAL — `geometry/components/basket.py` is a thin re-export — and every architecture produces one connected solid spanning the same vertical extent, so choosing one never moves the stone. The component is named `basket_support` for all of them. `TRELLIS` is not an enum member. | `TRELLIS` (needs swept geometry); rails; double gallery. |
| Explicit prong layouts and groups (Sprint 23) | current | `backend/jewelmind/setting/prong.py` | `test_setting_v2.py` (96) | `positionSource='EXPLICIT'` takes positions verbatim — the escape hatch for a layout no strategy derives — and `ProngGroupSpec` overrides style per prong index. Explicit and derived layouts never mix: a half-overridden layout has no determinate meaning. | Automatic shared-prong layout. |
| Setting → stone-instance mapping (Sprint 23) | current | `backend/jewelmind/setting/models.py`, `prong.py` | `test_setting_v2.py` (96) | `stoneInstanceAssignments` maps each generated component to the stone instance IDs it serves, by ID and sorted, so a shared prong reports both stones instead of a consumer inferring sharing from coordinates. The Setting System carries the references and never imports the arrangement layer (AST-enforced). | Generation against two stones. |
| Reference seat relief (Sprint 23) | partial | `backend/jewelmind/setting/seat.py` | `test_setting_v2.py` (96) | Opt-in `REFERENCE_SEAT` uses the real stone solid as a CUTTING TOOL against production metal, so metal no longer occupies the stone's volume. Never a fuse — LAW-006 holds, asserted by parsing `seat.py`'s own source. PARTIAL because it is relief, NOT a cut seat with a bearing shoulder, and no claim is made that a stone would sit correctly in it. Off by default. | A real bearing; cutter geometry (both PLANNED, no sourced geometry). |
| Shared prong geometry (Sprint 23) | partial | `backend/jewelmind/setting/prong.py` | `test_setting_v2.py` (96) | A prong serving two stones is representable and its assignment reported, but cannot be generated against two stones: one stone component is built per model. Blocked on the same dependency as multi-stone geometry — see [`25-setting-v2/head-execution-boundary.md`](../25-setting-v2/head-execution-boundary.md). | A per-instance stone transform; instance-aware inspection. |
| Stone arrangement representation (Sprint 22) | current | `backend/jewelmind/arrangement/`, `domain/schema.py::JewelryDefinition.arrangement` | `test_arrangement.py` (104), `test_arrangement_schemas.py` (43), `test_arrangement_no_category_dependency.py` (11), `arrangementValidation.test.ts` (13) | First-class stone instances with stable ids, groups, LINEAR/RADIAL/MIRROR patterns and five declared relation kinds. Category- and kernel-neutral, proven by AST inspection. Identity is by id, never array position. See [`24-arrangement/README.md`](../24-arrangement/README.md). | Instance-relative placement; enforced relations. |
| Deterministic arrangement resolution (Sprint 22) | current | `backend/jewelmind/arrangement/resolve.py`, `normalize.py` | `test_arrangement.py` (104) | Closed-form pattern expansion and frame composition in one fixed-order pass — no solver, no iteration, no random ids. Repeated resolution is byte-identical. `arrangementFingerprint` is separate from both `definitionHash` and `geometryHash`. | A pattern along an arbitrary curve (PLANNED). |
| Arrangement structural validation (Sprint 22) | current | `backend/jewelmind/validation/engine.py::_arrangement_rules` | `test_arrangement.py` (104), `test_validation.py` | `JM-ARRANGE-001..006`, structural and referential only. `JM-ARRANGE-004` runs the real resolver so Forge cannot disagree with generation. No spacing, proportion or density rule exists; stone overlap is a geometric question for Inspection. | Geometric checks once multi-stone geometry exists. |
| Arrangement compilation boundary (Sprint 22) | current | `backend/jewelmind/arrangement/compile.py`, `geometry/model.py::GeneratedModel.arrangement_result` | `test_arrangement.py` (104), `test_arrangement_schemas.py` (43) | Reports every resolved instance's generation status; `NOT_GENERATED` always carries a reason and `GENERATED` always names its component, both enforced by a model validator. The primary instance maps to the historical `stone_reference` component. | Emission for additional instances. |
| Multi-stone geometry generation (Sprint 22) | partial | `backend/jewelmind/arrangement/compile.py`, `geometry/roles.py` | `test_arrangement.py` (104) | An eight-stone halo resolves to eight real placements and produces ONE stone solid; the other seven are reported `NOT_GENERATED` with a reason. No placeholder geometry is emitted. The `stone_reference.<instanceId>` naming contract and its role classification are in place and tested, so an additional stone can never be misclassified as production metal. Blocked on a per-instance stone transform and instance-aware Geometry Inspection — see [`24-arrangement/execution-boundary.md`](../24-arrangement/execution-boundary.md). | A per-instance stone transform; instance-aware inspection; an accent setting strategy (RFC). |
| Gem registry and identity (Sprint 21) | current | `backend/jewelmind/gem/`, `domain/schema.py::StoneSpec.gem` | `test_gem_identity.py` (119), `test_gem_no_category_dependency.py` (9), `test_gem_api.py` (16), `GemControls.test.tsx` (9) | 40 registry entries and a per-stone `GemIdentity`, split type/instance so an entry describes a KIND of gem and the identity describes one stone. An internal taxonomy, not a gemological database: no hardness, durability, heat-sensitivity, treatment-safety or setting-suitability data exists, and no entry is certified. Every entry is `NOT_REVIEWED`. See [`23-gem-identity/README.md`](../23-gem-identity/README.md). | Sourced gem data; professional review. |
| Origin and treatment declaration (Sprint 21) | current | `backend/jewelmind/gem/models.py`, `validation/engine.py::_gem_rules` | `test_gem_identity.py` (119) | Independent axes: a stone may be natural AND treated, or synthetic AND untreated. Three distinct states are preserved — nothing recorded, declared untreated, and treated-but-unspecified. Declaration only: `disclosure` records who claimed it, and nothing verifies a claim. | A treatment-evidence model. |
| Gem visual profiles (Sprint 21) | current | `backend/jewelmind/gem/visual.py`, `frontend/src/vision/gemMaterials.ts` | `test_gem_identity.py` (119), `gemMaterials.test.ts` (11) | 20 profiles driving the stone's appearance from its gem, in both Technical and Presentation views. Every value is a RENDERING parameter — `ior` is not a refractive index. An unrecognized profile renders a neutral fallback, never a diamond look. | Measured optics (PLANNED). |
| Geometry/identity hash separation (Sprint 21) | current | `backend/jewelmind/utils/hashing.py::geometry_hash`, `services/model_service.py` | `test_gem_identity.py` (119), `test_gem_api.py` (16) | `definitionHash` covers the whole document; `geometryHash` excludes the fields empirically verified not to affect geometry, so a semantic-only edit reuses already-built geometry instead of re-running the kernel. Every exclusion was verified by generating geometry with that field varied. | A `compilationHash` (Alchemist). |
| Gem language in Designer and Conversation (Sprint 21) | current | `backend/jewelmind/designer/gem_language.py`, `conversation/references.py` | `test_gem_designer_language.py` (45) | English and Italian gem, origin and treatment terms, derived from the live registry. A term naming both a cut and a species (`emerald`, `pearl`) is reported ambiguous rather than resolved, and an unrecognized gem produces a clarification offering `custom`/`unknown` rather than an unsupported-feature report. No new Conversation action type was needed. | Multi-gem phrasing for arrangements. |
| Multi-stone gem arrangement (Sprint 21) | planned | `backend/jewelmind/gem/models.py::StoneInstance` | `test_gem_identity.py` (119) | The MODEL exists — an instance ID, a role and its own gem — but no generator builds more than one stone and `JewelryDefinition` has no `stones` field. Recorded as PLANNED so the model's existence is not read as a feature. | Halo, pavé and three-stone geometry. |
| Stone v2 runtime inspection (Sprint 20) | current | `backend/jewelmind/geometry/inspection/inspector.py` | `test_geometry_inspection.py`, `test_geometry_inspection_schemas.py` | 17 new fact types (registry now 47, v1.3.0) covering source mode, shape/profile/family/symmetry identity, representation, dimension provenance, outline and anchor availability, and provenance. Facts only — no fact is consumed by a Forge rule, and saying so is honest. | Forge consumption of the new facts. |
| Capability Coverage Guard (Sprint 19) | current | `specs/capabilities/jewelmind-capabilities.json` | `test_capability_coverage.py` (16) | 101 capabilities across 26 domains, checked against the live registries. `CURRENT` requires real implementation AND tests. | Extend with each new subsystem. |
| Stone orientation (Sprint 18) | current | `backend/jewelmind/geometry/stone/builder.py::_apply_orientation()` | `test_stone.py::TestStoneOrientation`, `TestPearAsymmetry` | Measured-dimension inspection facts isolate LENGTH from WIDTH exactly only at `orientation == 0`. Geometrically inert for round. | Orientation-aware measured facts — see [`20-stone/579-open-stone-questions.md`](../20-stone/579-open-stone-questions.md). |
| Four prongs | current | `backend/jewelmind/geometry/components/prongs.py` | `test_geometry.py::test_prongs_four_count`, `test_four_and_six_prong_models_visibly_differ` | Prongs are plain cylinders, not tapered/shaped. | None planned within current scope. |
| Six prongs | current | `backend/jewelmind/geometry/components/prongs.py` | `test_geometry.py::test_prongs_default_count_is_six` | Same as above. | None planned within current scope. |
| Basket support | current (deliberately simple) | `backend/jewelmind/geometry/components/basket.py` | `test_geometry.py::test_basket_exists_and_has_positive_volume` | Plain cylindrical shell, not decorative. | None planned within current scope. |
| Complete solitaire assembly | current | `backend/jewelmind/geometry/assemblies/solitaire.py` | `test_geometry.py::test_solitaire_assembly_has_all_required_components`, `test_solitaire_assembly_metal_is_single_fused_solid_by_default`, `test_definition_hash_is_deterministic` | Falls back to a multi-solid compound if boolean fuse fails (see [LAW-005](004-jewelmind-constitution.md)). | None planned within current scope. |
| Browser 3D preview | current | `frontend/src/components/ModelViewport.tsx`, `frontend/src/hooks/useComponentGeometries.ts` | `useComponentGeometries.test.ts` (7) | Single fixed set of materials/colors per metal; no lighting customization. | None planned within current scope. |
| STEP export | current | `backend/jewelmind/exporters/step_exporter.py` | `test_api.py::test_export_step_returns_nonempty_file`, `test_api_hardening.py` (unique-temp-file tests) | Excludes stone by default (by design, [LAW-006](004-jewelmind-constitution.md)). | None planned within current scope. |
| STL export | current | `backend/jewelmind/exporters/stl_exporter.py` | `test_api.py::test_export_stl_returns_nonempty_file`, `test_api_hardening.py` (tolerance validation tests) | Same exclusion as STEP. | None planned within current scope. |
| JSON export | current | `backend/jewelmind/exporters/json_exporter.py` | `test_api.py::test_export_json_matches_original_definition` | None known. | None planned. |
| Technical specification export | current | `backend/jewelmind/exporters/specification.py` | `test_api.py::test_specification_export_contains_disclaimer`, `test_api_hardening.py::test_specification_uses_original_generation_timestamp_not_download_time` | Markdown only, no PDF. | PDF rendering if requested. |
| `localStorage` persistence | current | `frontend/src/store/persistence.ts`, `shared/types/jewelry-definition.ts::isValidJewelryDefinition` | `persistence.test.ts` (14) | Single-slot (one saved project at a time), no project history. | Multiple saved projects, if requested. |
| Docker Compose setup | current (configuration reviewed, not live-tested in this pass) | `docker-compose.yml`, `backend/Dockerfile`, `frontend/Dockerfile` | Exercised by the `docker-smoke-test` job in `.github/workflows/ci.yml` | Frontend image runs the Vite dev server, not a production nginx build (`docs/known-limitations.md`). | Multi-stage production frontend image. |
| GitHub Actions CI | current | `.github/workflows/ci.yml` | Runs `pytest`, `ruff`, frontend lint/test/build, and the Docker smoke test on every push/PR to `main` | None known. | None planned. |
| API surface | current | `backend/jewelmind/api/routes.py`, `schemas.py` | `test_api.py` (15), `test_api_hardening.py` (17) | See [`appendices/api-inventory.md`](../appendices/api-inventory.md) for the full endpoint list. | None planned within current scope. |
| Error handling | current | `backend/jewelmind/api/errors.py`, `app.py` | `test_api_hardening.py::test_generation_failure_maps_to_model_generation_failed`, `test_step_export_failure_maps_to_step_export_failed`, `test_stl_export_failure_maps_to_stl_export_failed`, `test_cad_engine_unavailable_returns_503` | None known. | None planned. |
| Temporary file management | current | `backend/jewelmind/services/model_service.py` | `test_api_hardening.py::test_export_temp_file_is_deleted_after_http_response`, `test_export_temp_file_is_cleaned_up_on_failure`, `test_step_and_stl_exports_use_distinct_unique_temp_files` | In-memory model cache capped at 20 entries, cleared on backend restart. | Persistent model cache, if requested. |
| Conversation Engine v1 (multi-turn natural-language design refinement) | current | `backend/jewelmind/conversation/` (`__init__.py`, `errors.py`, `schemas.py`, `state.py`, `references.py`, `clarifications.py`, `context.py`, `service.py`), `backend/jewelmind/api/routes.py::conversation_turn_route`, `frontend/src/components/ConversationPanel.tsx`, `frontend/src/store/useConversationStore.ts` | `test_conversation.py` (26), `test_conversation_engine.py` (15, incl. the 6 required CASE A-F scenarios), `test_conversation_api.py` (6), `test_conversation_corpus.py` (82, incl. 80 real multi-turn cases across 17 categories), `test_conversation_schemas.py` (8); frontend `ConversationPanel.test.tsx` (7), `useConversationStore.test.ts` (3) | Backend is stateless per request — no server-persisted `ConversationSession`; `ADD_INTENT`/`REMOVE_INTENT` action types and `WAITING_FOR_ACCEPTANCE`/`CLOSED`/`FAILED` session statuses are schema-complete but currently unreachable (see `appendices/conversation-action-catalog.md`, `appendices/conversation-state-catalog.md`). | None planned within current scope. |
| Professional Validation Framework v1 (structured evidence capture for real professional review) | current (infrastructure only — zero real validations exist) | `backend/jewelmind/professional_validation/` (`__init__.py`, `errors.py`, `schemas.py`, `registry.py`, `scope.py`, `versioning.py`, `review_package.py`, `cli.py`), `backend/jewelmind/api/routes.py::review_package_route`, `frontend/src/components/ProfessionalReviewPanel.tsx` | `test_professional_validation_schemas.py` (37), `test_professional_validation_registry.py` (7), `test_professional_validation_cli.py` (12), `test_professional_validation_scope.py` (5), `test_professional_validation_specs.py` (8), `test_professional_validation_versioning.py` (6), `test_review_package.py` (14), `test_review_package_api.py` (5); frontend `ProfessionalReviewPanel.test.tsx` (6) | The active registry (`specs/professional-validation/v1/current-validation-registry.json`) contains zero records by design — no real jewelry professional has reviewed anything yet; this framework only builds the capture mechanism. See [`15-professional-validation/451-validation-gap-analysis.md`](../15-professional-validation/451-validation-gap-analysis.md). | Real pilot reviews once qualified reviewers are engaged — see [`15-professional-validation/README.md`](../15-professional-validation/README.md)'s pilot plan. |

## Test totals (as of this document's `last_updated`)

- **Backend:** 139 tests across `test_api.py` (15), `test_api_hardening.py`
  (17), `test_geometry.py` (14), `test_schema.py` (3),
  `test_schema_safety.py` (70), `test_validation.py` (20). Command:
  `cd backend && .venv/Scripts/python -m pytest -q`.
- **Frontend:** 41 tests across `BackendStatus.test.tsx` (3),
  `ConfigurationPanel.test.tsx` (3), `JsonViewer.test.tsx` (2),
  `ProjectActions.test.tsx` (5), `ValidationPanel.test.tsx` (3),
  `useComponentGeometries.test.ts` (7), `persistence.test.ts` (14),
  `useProjectStore.test.ts` (4). Command: `cd frontend && npm run test`.

See [`appendices/test-inventory.md`](../appendices/test-inventory.md) for
what each suite actually covers and where coverage is known to be thin.

## Jewelry-domain detail

The table above is a capability-level summary. For the underlying
jewelry-domain concepts — what a "ring," "band," "stone reference,"
"setting," "prong," and "basket support" actually mean in this codebase,
which are IMPLEMENTED FACTs versus PRELIMINARY SOFTWARE RULEs, and which
jewelry-domain questions remain open — see
[`04-jewelry-domain/README.md`](../04-jewelry-domain/README.md) (Sprint 2
of this Bible). In particular,
[`04-jewelry-domain/054-domain-validation-classification.md`](../04-jewelry-domain/054-domain-validation-classification.md)
classifies every one of the sixteen validation rules referenced above,
and confirms that **zero** of them have been professionally validated as
of this Sprint.
